workspace = { members = ["minigrep_lib"] }
[package]
name = "minigrep"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
minigrep_lib = {path = "minigrep_lib"}